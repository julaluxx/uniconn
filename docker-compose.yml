services:
  xampp:
    image: tomsik68/xampp:latest
    container_name: uc-server-2
    privileged: true
    ports:
      - "8088:80"
    volumes:
      - /workspaces/UniConn:/opt/lampp/htdocs/uniconn:Z
      # - ~/Documents/uniconn:/opt/lampp/htdocs/uniconn:Z
    restart: unless-stopped
